Package.describe({
  summary: 'dunnhumby notifications package',
  version: '0.1.0',
  name: 'dh:notifications'
});

Package.onUse(function (api) {

  // ---------------------------------- 1. Core dependency -----------------------------------

  // api.use("raix:push");

  // ---------------------------------- 2. Files to include ----------------------------------

  // // client

  // api.addFiles([
  //   'lib/security.js'
  // ], ['client']);

});
